# 账号切流功能需求文档

## 原始需求
**拦截账号的响应，判断响应是否正常，如果不正常，则将请求到分组内另一个账号去处理。如果分组内的账号都无法正常处理这个请求，那么使用默认账号去处理这个请求。**

## 详细需求分析

### 核心功能
1. **响应拦截** - 在请求返回前检查响应状态
2. **异常判断** - 识别哪些响应状态需要触发切换（401、429、5xx等）
3. **分组内切换** - 优先切换到同分组内的其他健康账号
4. **默认账号兜底** - 分组内账号都失败时使用默认后备账号
5. **请求重试** - 透明地重新发送原始请求到新账号

### 触发条件
- HTTP状态码：401（未授权）、429（限流）、5xx（服务器错误）
- 连接错误：ECONNRESET、ENOTFOUND、TIMEOUT
- 业务错误：特定的error类型和错误消息

### 切换策略
1. **第一优先级**：分组内其他健康账号
2. **第二优先级**：跨分组的健康账号（如果配置允许）
3. **最后兜底**：默认后备账号

### 会话保持
- 切换后的新会话映射应该更新
- 保持请求的完整性（headers、body等）

## 当前实现现状

### ✅ 已实现的功能
- 智能分组调度器（smartGroupScheduler.js）
- 账户健康检查机制（定期5分钟）
- 会话粘性和映射管理
- 基础的错误处理（401、429）
- 后备账户配置和选择

### ❌ 缺失的关键功能
1. **响应拦截器** - 当前没有在请求返回前检查响应并重试
2. **智能重试策略** - 缺少请求级别的账户切换和重试逻辑
3. **分组级联故障转移** - 分组失败后的跨分组或默认账户切换

### 🔄 需要实现的功能
根据用户明确要求，需要实现以下3个核心功能：

1. **响应拦截器** - 在claudeRelayService中添加响应检查和重试逻辑
3. **智能重试策略** - 支持指数退避和请求级别的账户切换
4. **分组级联故障转移** - 分组失败自动尝试其他分组或默认账户

## 技术实现要点

### 1. 响应拦截器实现
- 在axios响应拦截器中检查状态码
- 识别需要重试的错误类型
- 保存原始请求信息用于重试

### 2. 智能重试策略
- 最大重试次数限制（建议3次）
- 指数退避延迟（100ms, 200ms, 400ms）
- 排除已失败的账户ID
- 更新会话映射到新账户

### 3. 分组级联故障转移
- 分组内账户全部失败时触发
- 尝试跨分组选择（如果允许）
- 最终使用默认后备账户
- 记录故障转移日志

## 成功标准
- 单个账户故障时能自动切换到分组内其他账户
- 分组内账户全部故障时能切换到默认账户  
- 请求对客户端透明，无需客户端感知切换过程
- 响应时间增加不超过1秒
- 切换成功率达到95%以上

## 测试验证
- 使用已创建的test-failover.js脚本进行验证
- 模拟各种故障场景
- 验证切换逻辑和响应时间
- 确保会话一致性

---
*文档创建时间: 2025-09-03*
*需求确认: 用户明确只需要功能1、3、4，不需要实时健康监控*